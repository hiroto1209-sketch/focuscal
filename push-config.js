// FocusCal Web Push public configuration.
// Paste the Web Push certificate key pair PUBLIC key (VAPID key) from Firebase Console here.
// This key is public by design and is safe to ship to the browser.
window.FOCUSCAL_FCM_VAPID_KEY = window.FOCUSCAL_FCM_VAPID_KEY || '';
